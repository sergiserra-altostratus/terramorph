resource "google_compute_security_policy" "{{ resource.terraform_resource_name }}" {
  project = "{{ resource.project }}"
  name    = "{{ resource.name }}"
{% if attrs.description %}
  description = "{{ attrs.description }}"
{% endif %}
}
