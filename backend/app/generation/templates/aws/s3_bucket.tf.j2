resource "aws_s3_bucket" "{{ resource.terraform_resource_name }}" {
  bucket = "{{ attrs.bucket }}"
{% if attrs.tags %}

  tags = {
{% for key, value in attrs.tags.items() %}
    {{ key }} = "{{ value }}"
{% endfor %}
  }
{% endif %}
}
