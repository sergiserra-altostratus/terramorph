resource "aws_eip" "{{ resource.terraform_resource_name }}" {
  domain = "{{ attrs.domain }}"
{% if attrs.tags %}

  tags = {
{% for key, value in attrs.tags.items() %}
    {{ key }} = "{{ value }}"
{% endfor %}
  }
{% endif %}
}
